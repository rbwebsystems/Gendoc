{
  "name": "sened-generator",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test:cash-sync": "node --test tests/cash-sync.test.mjs",
    "test:credit-assessment": "node --test tests/credit-assessment.test.mjs",
    "test:sync-timing": "node --test tests/sync-timing.test.mjs",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "preview:open": "vite preview --open --host",
    "build:preview": "npm run build && vite preview --open --host"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "html2pdf.js": "^0.14.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "read-excel-file": "^9.3.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.7.2",
    "vite": "^6.0.7"
  }
}
